import 'package:flutter/material.dart';

class DecouvrirScreen extends StatelessWidget {
  const DecouvrirScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('decouvrir'),
    );
  }
}
