import 'package:flutter/material.dart';

class CodePin extends StatelessWidget {
  const CodePin({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Launch the game code pin'));
  }
}
